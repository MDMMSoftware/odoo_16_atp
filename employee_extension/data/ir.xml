<odoo>
    <data noupdate="1">
        <!-- Define the server action -->
        <record id="remove_users_from_res_groups" model="ir.actions.server">
            <field name="name">Remove All Users from a Access Group</field>
            <field name="model_id" ref="base.model_res_groups"/>
            <field name="state">code</field>
            <field name="code">
                <![CDATA[
                for rec in records:
                    rec.write({'users': [(6,0, [])]})
                ]]>
            </field>
        </record>

    </data>
</odoo>
